appId: com.anonymous.example
---
- retry:
    maxRetries: 3
    commands:
      - launchApp
      - tapOn: "Spending"
      - assertVisible: "Spending"
      - assertVisible: "Income"
      - assertVisible: "Total.*"
